#pragma once


//is a tracking board nessecary or can an opponent just display a version of it's board that is a trakcing board 
//instead of passing info from their board to a tracking board.
enum class TrackingTile {Unknown, Hit, Miss, Sunk};


class TrackingBoard {
public:

private:

};